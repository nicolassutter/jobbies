services:
  auth:
    container_name: jobbies_auth_pocketbase
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8080:8080
    volumes:
      - ./pb_data:/pb/pb_data

